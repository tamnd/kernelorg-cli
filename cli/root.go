// Package cli builds the kernel command tree on top of the kernelorg library.
package cli

import (
	"github.com/spf13/cobra"
)

// Build metadata, set via -ldflags at release time.
var (
	Version = "dev"
	Commit  = "none"
	Date    = "unknown"
)

// Root builds the root command and its subtree.
func Root() *cobra.Command {
	root := &cobra.Command{
		Use:   "kernel",
		Short: "Browse Linux kernel releases from kernel.org",
		Long: `Browse Linux kernel releases from kernel.org

This is a fresh scaffold. Add your commands here on top of the kernelorg
library package, then wire them into Root with root.AddCommand.`,
		SilenceUsage:  true,
		SilenceErrors: true,
	}

	root.AddCommand(newVersionCmd())
	// TODO: root.AddCommand(newGetCmd()), etc.
	return root
}
